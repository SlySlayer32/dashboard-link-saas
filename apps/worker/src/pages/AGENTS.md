# Worker Pages Agent Guide

## Scope
Route-level screens for worker flows.

## Rules
- Pages orchestrate hooks + components and handle loading/error/empty states.
- Keep token handling and routing logic here, not in components.

## Touchpoints
- Hooks: `apps/worker/src/hooks`
- Components: `apps/worker/src/components`

## Tests
- Add page tests if flows are complex.

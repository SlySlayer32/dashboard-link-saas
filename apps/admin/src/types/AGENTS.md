# Admin Types Agent Guide

## Scope
Admin-only types and type helpers.

## Rules
- Prefer shared types from `@dashboard-link/shared`.
- Keep app-specific types close to their usage; avoid duplicates.

## Touchpoints
- Shared contracts: `packages/shared`.

## Tests
- No direct tests; types validated via TypeScript.

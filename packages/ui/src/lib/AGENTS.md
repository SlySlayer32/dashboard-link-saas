# UI Lib Agent Guide

## Scope
Internal helpers used by UI components and hooks.

## Rules
- Keep helpers small and reusable.
- Avoid app-specific logic and side effects.

## Touchpoints
- Used by `packages/ui/src/components` and `packages/ui/src/hooks`.

## Tests
- Add unit tests for non-trivial helpers.

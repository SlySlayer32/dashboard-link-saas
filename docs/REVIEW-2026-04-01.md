# Code Review — 2026-04-01

**Scope**: Full codebase review on `main` branch (commit `433c99a`)
**Reviewer**: Cascade (automated)
**Constitution version**: 1.0.0

---

## Findings

| ID | Pass | Layer | Type | Severity | Location | Description | Recommended Action |
|----|------|-------|------|----------|----------|-------------|-------------------|
| R01 | 1 | constitution | SECURITY | CRITICAL | `packages/shared/src/tenant-middleware.ts:L17-33` | JWT `verify()` function decodes payload without signature verification — `_secret` param is unused. Any fabricated JWT is accepted. All v1 routes rely on this for tenant identity. | Implement proper JWT verification using `jose` or Supabase's `getUser()`. Never decode JWTs without verifying signature. |
| R02 | 1 | constitution | BUG | CRITICAL | `apps/api/src` (no call to `initializeContainer`) | `initializeContainer()` is never called anywhere in the API app. `getWorkerRepository()` is invoked at module-level in `routes/workers.ts:L23`, meaning the app crashes on startup with "Container not initialized". | Add `initializeContainer()` call in `apps/api/src/index.ts` before routes are imported, or use lazy initialization. |
| R03 | 1 | constitution | SECURITY | CRITICAL | `apps/api/src/routes/tokens.ts:L8-11` | Token routes use `SUPABASE_SERVICE_KEY` (bypasses RLS) with direct Supabase calls. Combined with R01 (forged JWT), an attacker can access/modify any organization's tokens. | Move token queries to a `TokenRepository` in `packages/database`, and ensure proper auth middleware is applied. |
| R04 | 1 | constitution | SECURITY | CRITICAL | `apps/api/src/middleware/cache.ts:L90-104` | Cache key is `URL + varyParams` with no tenant/organization scoping. Two tenants requesting the same endpoint get each other's cached data — complete multi-tenant data leak. | Include `organizationId` or tenant identifier in the cache key. |
| R05 | 1 | constitution | VIOLATION | HIGH | `apps/api/src/v1.ts:L320-332, L457-465` | Direct Supabase queries in route handlers (`dashboards`, `adapter_configs`). Constitution §I: "All database access through repositories — no direct SQL in route handlers." | Create `DashboardRepository` query methods and `AdapterConfigRepository`. Route handlers should call service/repository only. |
| R06 | 3 | implementation | DUPLICATION | HIGH | `packages/shared/src/tenant-middleware.ts`, `apps/api/src/middleware/tenant.ts`, `apps/api/src/middleware/auth.ts` | Three separate tenant/auth middleware implementations with different behaviors. v1 routes use the shared one (broken JWT); workers route uses the api-local ones (different auth flow). | Canonicalize to a single auth + tenant middleware chain. See Canonical Path Recommendations below. |
| R07 | 1 | constitution | VIOLATION | HIGH | `apps/api/src/index.ts:L59-61` vs `apps/api/src/v1.ts:L34-41` | Middleware order violation. Constitution §I: "Logger → CORS → Tenant → Cache → Routes → Error". Cache middleware is applied in `index.ts` before tenant middleware runs in `v1.ts`. Cache runs on unauthenticated requests. | Move cache middleware inside v1 router, after tenant middleware. |
| R08 | 1 | constitution | VIOLATION | HIGH | `apps/api/src/services/TokenService.ts:L6,10`, `SMSService.ts:L6`, `packages/database/src/repositories/WorkerRepository.ts:L214-215`, `packages/database/src/di/Container.ts:L55` | `any` type used in multiple locations. Constitution §I: "Never use `any` — use `unknown` or proper types." | Replace all `any` with proper types or `unknown`. |
| R09 | 5 | implementation | BUG | HIGH | `apps/api/src/v1.ts:L31` | Supabase client created with `SUPABASE_ANON_KEY` (subject to RLS). The shared tenant middleware tries to set `app.tenant_id` via `c.env.DB.execute()` which doesn't exist in Node.js runtime. Queries via this client will return empty or fail. | Use service key client with explicit tenant context setting via `set_tenant_context()` RPC, or move to repository pattern. |
| R10 | 5 | implementation | BUG | HIGH | `packages/shared/src/tenant-middleware.ts:L85-87, L137-153` | `dashboardTokenMiddleware` and tenant context setting use `c.env.DB.prepare()`/`c.env.DB.execute()` — Cloudflare Workers D1 APIs, not compatible with Node.js + Supabase stack. Dead code that will throw at runtime. | Rewrite using Supabase client RPC calls (`supabase.rpc('set_tenant_context', ...)`) compatible with Node.js runtime. |
| R11 | 5 | implementation | BUG | HIGH | `apps/api/src/routes/workers.ts:L330, L352` | Routes `GET /search/:query` and `GET /active/list` are unreachable. Hono matches routes in registration order — `GET /:id` (line 140) catches `/search/john` and `/active/list` first, treating `"search"` and `"active"` as the `:id` param. | Move `/search/:query` and `/active/list` routes BEFORE `/:id`, or restructure as query parameters on the list endpoint (already partially done). |
| R12 | 5 | implementation | SECURITY | MEDIUM | `supabase/migrations/20260315080001_tenant_context_function.sql:L14` | `GRANT EXECUTE ON FUNCTION set_tenant_context(UUID) TO authenticated` — allows any authenticated user to set arbitrary tenant context, bypassing RLS. The older migration (`20260131000000`) correctly restricts `set_tenant_id` to `service_role` only. | Revoke `authenticated` grant on `set_tenant_context`. Only `service_role` should be able to set tenant context. |
| R13 | 5 | implementation | CONFLICT | MEDIUM | `supabase/migrations/20260131000000` vs `20260315080001` | Two duplicate tenant context functions: `set_tenant_id(TEXT)` and `set_tenant_context(UUID)`. Different signatures, different permission models. API middleware (`tenant.ts`) calls `set_tenant_context`, shared middleware calls `c.env.DB.execute`. | Pick one canonical function. Recommend `set_tenant_context(UUID)` with service_role-only grant. Drop `set_tenant_id`. |
| R14 | 5 | implementation | BUG | MEDIUM | `apps/api/src/routes/workers.ts:L81-89` | Double-filtering on `active` status. `getWorkersByActiveStatus()` already filters by active status, then line 88 re-filters with `.filter(worker => worker.active === query.active)`. Redundant and misleading. | Remove the redundant `.filter()` on line 88. |
| R15 | 5 | implementation | BUG | MEDIUM | `apps/api/src/services/WorkerService.ts:L519-525` | `bulkUpdateStatus` catches all errors and returns `null` without logging. Silent failure makes debugging impossible and violates error handling principles. | Add `logger.error()` in the catch block, or propagate errors with a summary. |
| R16 | 5 | implementation | CONFLICT | MEDIUM | `apps/api/src/index.ts:L67` vs `L76` | Version string inconsistency: root endpoint returns `0.1.0`, health endpoint returns `1.0.0`. | Unify to a single `APP_VERSION` constant. |
| R17 | 5 | implementation | VIOLATION | MEDIUM | `apps/api/src/middleware/rate-limit.ts:L62-68` | Rate limit error response uses `{ error: string }` format instead of the standard `{ success: false, error: { code, message } }` per API Patterns in essential-rules. | Update to match standard error response format. |
| R18 | 5 | implementation | IMPROVEMENT | LOW | `apps/api/src/index.ts:L49-55` | CORS config always includes `http://localhost:5173` and `http://localhost:5174` hardcoded. In production these dev origins would still be allowed. Constitution: "Never use `*` in production" — these hardcoded dev origins are nearly as bad. | Use `config.cors.origin` from env.ts (already defined there), and conditionally include localhost only when `NODE_ENV !== 'production'`. |
| R19 | 5 | implementation | IMPROVEMENT | LOW | `apps/api/src/middleware/cache.ts:L31` | `MemoryCache.set()` accepts a `_ttl` parameter but ignores it — always uses `defaultTTL`. Callers passing custom TTL get unexpected behavior. | Either use the `_ttl` parameter to set per-entry TTL, or remove it from the signature. |

---

## Canonical Path Recommendations

### Auth + Tenant Middleware Chain

**Keep**: `apps/api/src/middleware/auth.ts` — proper JWT validation via auth service abstraction
**Keep**: `apps/api/src/middleware/tenant.ts` — sets RLS context via Supabase RPC
**Remove**: `packages/shared/src/tenant-middleware.ts` `tenantMiddleware` function — broken JWT verification, D1-specific APIs
**Update**: `apps/api/src/v1.ts` — replace `tenantMiddleware` from `@dashboard-link/shared` with `authMiddleware` + `tenantContextMiddleware` from local middleware
**Update**: `packages/shared/src/index.ts` — stop exporting `tenantMiddleware` (keep `TenantContext` type only)

---

### Tenant Context SQL Function

**Keep**: `set_tenant_context(UUID)` from migration `20260315080001` — correct UUID typing
**Remove**: `set_tenant_id(TEXT)` from migration `20260131000000` — redundant, weaker typing
**Update**: `20260315080001` — revoke `authenticated` grant, keep `service_role` only (matches security model of the older migration)

---

### Direct Supabase Client in Routes

**Keep**: Repository pattern via DI Container (`packages/database/src/di/Container.ts`)
**Remove**: Direct `createClient()` calls in `apps/api/src/v1.ts:L31` and `apps/api/src/routes/tokens.ts:L8-11`
**Update**: Create `TokenRepository` and `AdapterConfigRepository` in `packages/database/src/repositories/`

---

## Fix Tasks

### CRITICAL (block — fix before anything ships)

- [x] [REVIEW-2026-04-01-jwt-verify] CRITICAL: Implement proper JWT signature verification in shared tenant middleware or replace with auth middleware chain in `packages/shared/src/tenant-middleware.ts`
- [x] [REVIEW-2026-04-01-container-init] CRITICAL: Add `initializeContainer()` call in `apps/api/src/index.ts` before route imports to prevent crash on startup
- [x] [REVIEW-2026-04-01-token-rls] CRITICAL: Move token route queries from direct Supabase calls to repository pattern with proper auth middleware in `apps/api/src/routes/tokens.ts`
- [x] [REVIEW-2026-04-01-cache-tenant] CRITICAL: Include organization ID in cache key to prevent cross-tenant data leaks in `apps/api/src/middleware/cache.ts`

### HIGH (fix promptly)

- [x] [REVIEW-2026-04-01-direct-supabase] HIGH: Replace direct Supabase calls in `apps/api/src/v1.ts` (send-link, adapter configs) with repository pattern
- [x] [REVIEW-2026-04-01-middleware-dup] HIGH: Canonicalize tenant/auth middleware — remove shared tenant middleware, apply auth.ts + tenant.ts to all v1 routes
- [x] [REVIEW-2026-04-01-middleware-order] HIGH: Move cache middleware inside v1 router after tenant middleware to fix ordering violation
- [x] [REVIEW-2026-04-01-any-types] HIGH: Replace all `any` usage with proper types in TokenService, SMSService, WorkerRepository, Container
- [x] [REVIEW-2026-04-01-anon-key] HIGH: Fix v1.ts Supabase client — either use service key with explicit tenant context or use repository pattern
- [x] [REVIEW-2026-04-01-d1-apis] HIGH: Remove Cloudflare D1 API usage in shared tenant-middleware.ts — rewrite for Node.js + Supabase
- [x] [REVIEW-2026-04-01-unreachable-routes] HIGH: Reorder workers routes so `GET /search/:query` and `GET /active/list` are defined before `GET /:id`

### MEDIUM (address before next major feature)

- [x] [REVIEW-2026-04-01-tenant-grant] MEDIUM: Revoke `authenticated` grant on `set_tenant_context` — only service_role should set tenant context
- [x] [REVIEW-2026-04-01-dup-tenant-fn] MEDIUM: Remove duplicate `set_tenant_id(TEXT)` function, keep only `set_tenant_context(UUID)`
- [x] [REVIEW-2026-04-01-double-filter] MEDIUM: Remove redundant active status filter in workers route `L88`
- [x] [REVIEW-2026-04-01-silent-bulk] MEDIUM: Add error logging to `bulkUpdateStatus` catch block in WorkerService
- [x] [REVIEW-2026-04-01-version-mismatch] MEDIUM: Unify version strings in index.ts to single constant
- [x] [REVIEW-2026-04-01-ratelimit-format] MEDIUM: Update rate limit error response to match standard API response format

### LOW (address when convenient)

- [x] [REVIEW-2026-04-01-cors-dev] LOW: Conditionally include localhost CORS origins only in development
- [x] [REVIEW-2026-04-01-cache-ttl] LOW: Fix ignored `_ttl` parameter in MemoryCache.set()
